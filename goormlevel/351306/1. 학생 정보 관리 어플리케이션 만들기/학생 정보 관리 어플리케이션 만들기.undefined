tc = int(input())

students = []
for _ in range(tc):
	command, name = input().split()

	if command == "add":
		students.append(name)

	else:
		count = 0
		for student in students:
			if student.startswith(name):
				count += 1

		print(count)
				